class Solution:
    # @return a boolean
    def isPalindrome(self, x):
        i=1
        tmp=a%10
        while a>=1:
            a = a/10
            tmp+= (10**-i*)(a%10)
            i+=1
        return tmp
        